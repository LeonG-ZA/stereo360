# --- Generated model weights (194 MB) -------------------------------------
# Exported by scripts/export_onnx.py, and re-downloadable. Far too big for git,
# and nothing here is authored.
models/*
!models/README.md

# --- Vendored upstream checkout -------------------------------------------
# third_party/Video-Depth-Anything is a clone of someone else's repository,
# carrying its own .git and its own licence. It is cloned, not authored, so it
# is not ours to redistribute.
third_party/*
!third_party/README.md

# --- Media ----------------------------------------------------------------
# Source footage and renders. An 8K stereo render is ~400 MB a second.
*.mp4
*.mov
*.mkv
*.webm
*.insv
*.wav
*.m4a
*.jpg

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Editors / OS ---------------------------------------------------------
.claude/settings.local.json
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# --- Reference media ------------------------------------------------------
# The scoring harness in experiments/ measures against a 360 photo of a
# private house, and its stereo render. Keep them beside the checkout, not in
# it: see "Depth scoring harness" in findings.md, and STEREO360_INDOOR.
indoor.jpg
indoor_360_TB.jpg
